# Dynamic QR 真实用户验收矩阵

本矩阵用于正式环境的人工验收，不使用伪造扫描事件或虚构用户数据。测试账号应使用实际已授权的 QRKit 用户，测试链接应在验收结束后删除或停用。

| 场景 | 操作 | 预期结果 | 证据 |
|---|---|---|---|
| 创建 | 登录 `/dynamic-qr`，填写合法 slug、标签和 HTTPS 目标并保存 | 列表出现新链接，状态为 ACTIVE，公开路径可复制 | 链接 ID、创建时间 |
| 编辑 | 选择已保存链接，修改目标并保存 | 预览目标更新，列表更新时间刷新 | 更新后的目标 URL |
| 停用 | 点击 Pause link 后访问 `/r/:id` | Pages Function 返回 404，数据库 active=false | HTTP 状态与数据库状态 |
| 跳转 | 恢复链接后打开 `/r/:id` | 返回 302，Location 指向当前目标 | HTTP headers |
| 统计 | 打开公开路径后回到管理页并刷新统计 | Total scans、Last scan 和 14 日趋势出现真实事件 | D1 scan_events 记录 |
| 权限 | 使用另一用户访问第一个用户的链接管理入口 | 不应返回对方链接或统计数据 | UI 列表和 API 响应 |
| 异常 | 使用无效目标、重复 slug、缺失链接和暂停链接 | 表单校验、冲突提示和 404 边界均可理解 | 截图或响应 |

管理页内置的 Acceptance flow 清单只记录当前浏览器中的验收进度，不替代生产环境的 HTTP、D1 和权限证据。
