/* QRKit Signal Paper: authenticated management uses the built-in OAuth and tRPC stack; unauthenticated visitors can still preview a local draft without persistence. */
import { useState } from 'react';
import { ArrowUpRight, LockKeyhole, LogIn, Save } from 'lucide-react';
import { Link } from 'wouter';
import { useAuth } from '../_core/hooks/useAuth';
import { startLogin } from '../const';
import RouteSeo from '../components/RouteSeo';
import { trpc } from '../lib/trpc';
import { dynamicRedirectUrl, type DynamicQrDraft, validateDynamicQrDraft } from '../lib/dynamicQrAdmin';

export default function DynamicQrPage(){const {user,isAuthenticated,loading}=useAuth();const utils=trpc.useUtils();const links=trpc.dynamicQr.list.useQuery(undefined,{enabled:isAuthenticated});const create=trpc.dynamicQr.create.useMutation({onSuccess:()=>{utils.dynamicQr.list.invalidate();setNotice('Dynamic QR link saved to your account.')}});const update=trpc.dynamicQr.update.useMutation({onSuccess:()=>utils.dynamicQr.list.invalidate()});const [draft,setDraft]=useState<DynamicQrDraft>({id:'spring-campaign',label:'Spring campaign',destination:'https://qrkit.example/campaign',active:true});const [notice,setNotice]=useState('');const error=validateDynamicQrDraft(draft);const updateDraft=(key:keyof DynamicQrDraft,value:string|boolean)=>setDraft(current=>({...current,[key]:value}));const save=()=>{if(!isAuthenticated){startLogin();return}create.mutate({slug:draft.id,label:draft.label,destination:draft.destination,active:draft.active})};return <main className="container py-16"><RouteSeo title="Dynamic QR Workspace | QRKit" description="Create and manage redirect-based QR links with QRKit's authenticated workspace." path="/dynamic-qr"/><div className="max-w-3xl"><p className="eyebrow">09 / Dynamic workspace</p><h1 className="mt-4 font-display text-5xl font-extrabold tracking-[-.07em] md:text-7xl">Manage a destination without reprinting the code.</h1><p className="mt-6 text-lg leading-8 text-[#6f746d]">Create a redirect-based QR link, pause it when needed, and keep the destination editable from your authenticated workspace.</p></div><div className="mt-12 grid gap-8 lg:grid-cols-[.85fr_1.15fr]"><section className="paper-panel rounded-3xl border p-6 md:p-8"><div className="flex items-center gap-3 rounded-xl bg-[#dce9e5] p-4 text-sm text-[#0e5b5d]"><LockKeyhole size={18}/><span>{loading?'Checking authentication…':isAuthenticated?`Signed in as ${user?.name||user?.email||'QRKit member'}.`:'Draft mode / sign in to persist this link.'}</span></div><div className="mt-8 grid gap-5"><label><span className="mb-2 block font-mono text-[.68rem] uppercase tracking-wider text-[#6f746d]">Slug</span><input className="field" value={draft.id} onChange={event=>updateDraft('id',event.target.value)} pattern="[a-z0-9-]+"/></label><label><span className="mb-2 block font-mono text-[.68rem] uppercase tracking-wider text-[#6f746d]">Label</span><input className="field" value={draft.label} onChange={event=>updateDraft('label',event.target.value)}/></label><label><span className="mb-2 block font-mono text-[.68rem] uppercase tracking-wider text-[#6f746d]">Destination URL</span><input className="field" value={draft.destination} onChange={event=>updateDraft('destination',event.target.value)} type="url"/></label><label className="flex items-center justify-between rounded-xl border border-[#d7d0c4] p-4"><span><span className="block font-display font-bold">Link active</span><span className="mt-1 block text-sm text-[#6f746d]">Inactive links return 404 from the redirect boundary.</span></span><input type="checkbox" checked={draft.active} onChange={event=>updateDraft('active',event.target.checked)} /></label></div>{error&&<p className="mt-5 text-sm font-medium text-red-700">{error}</p>}{!isAuthenticated?<button className="btn-primary mt-7" disabled={!!error||loading} onClick={save}><LogIn size={16}/> Sign in to save</button>:<button className="btn-primary mt-7" disabled={!!error||create.isPending} onClick={save}><Save size={16}/> {create.isPending?'Saving…':'Save Dynamic QR'}</button>}{create.error&&<p className="mt-4 text-sm text-red-700">{create.error.message}</p>}{notice&&<p className="mt-4 text-sm text-[#0e5b5d]">{notice}</p>}</section><section className="rounded-3xl border-2 border-[#0e5b5d] bg-[#dce9e5] p-6 md:p-8"><p className="eyebrow !text-[#0e5b5d]">Redirect preview</p><h2 className="mt-3 font-display text-3xl font-extrabold tracking-[-.05em]">{draft.label||'Untitled link'}</h2><div className="mt-8 rounded-2xl bg-[#fffdf8] p-5"><p className="font-mono text-xs uppercase tracking-wider text-[#6f746d]">Public path</p><code className="mt-3 block break-all text-sm text-[#0e5b5d]">{dynamicRedirectUrl(draft.id)}</code><p className="mt-5 font-mono text-xs uppercase tracking-wider text-[#6f746d]">Current target</p><p className="mt-2 break-all text-sm">{draft.destination}</p></div><div className="mt-6 flex items-center justify-between border-t border-[#0e5b5d]/20 pt-5"><span className="font-mono text-xs uppercase tracking-wider">{draft.active?'ACTIVE':'PAUSED'}</span><Link href="/pricing" className="inline-flex items-center gap-2 font-mono text-xs uppercase tracking-wider text-[#0e5b5d]">D1 / Pro setup <ArrowUpRight size={14}/></Link></div>{isAuthenticated&&<div className="mt-8 border-t border-[#0e5b5d]/20 pt-5"><p className="eyebrow !text-[#0e5b5d]">Your saved links</p>{links.isLoading?<p className="mt-3 text-sm">Loading…</p>:links.data?.length?<div className="mt-3 grid gap-2">{links.data.map(link=><button key={link.id} className="flex items-center justify-between rounded-lg bg-[#fffdf8] p-3 text-left text-sm" onClick={()=>{setDraft({id:link.slug,label:link.label,destination:link.destination,active:link.active});setNotice('Loaded saved link')}}><span className="font-medium">{link.label}</span><span className="font-mono text-xs text-[#0e5b5d]">{link.active?'ACTIVE':'PAUSED'}</span></button>)}</div>:<p className="mt-3 text-sm text-[#5f655e]">No saved links yet.</p>}</div>}</section></div></main>}
